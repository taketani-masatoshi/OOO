# EXTRACT — not canonical

This tree is a **comparison extract**. Do not edit it as product source.

| Role | Path |
|------|------|
| Canonical Console | `../Core/apps/wire-console`, `../Core/apps/steward-chat`, Core `src/lib/wire-console`, `src/lib/operator-console` |
| This folder | Review copy only (duplicate layouts under `wire-console/` and `apps/wire-console/` are intentional copies) |
| Build | `cd ../Core && npm run wire-console:build` (see BUILD.md) |

Development Agents must not treat this directory as a standard write target.
