# runtime-private

Local-only runtime data (logs, temp, secret placeholders).

**Never commit secrets.** Do not copy `.env`, `*.pem`, or `*.key` into tracked trees.
This directory is for private runtime material outside normal project sync.
