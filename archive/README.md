# archive/

Migration archaeology (`kind: archive`, `edit: false`). Not current how-to.

Tracked here when present:

- `MIGRATION-REPORT.md`
- `copy-exclusions.log`
- `legacy-*-status*.txt`

See `PROJECT-MAP.yaml` → `projects.archive`.
