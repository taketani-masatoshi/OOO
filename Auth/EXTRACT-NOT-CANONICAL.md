# EXTRACT — not canonical

This tree is a **comparison extract** of auth-related sources. It is not a shared auth package.

| Role | Path |
|------|------|
| Canonical implementations | Core Wire/Steward auth modules; Community `apps/web` NextAuth stack |
| This folder | Review copy for contracts inventory |
| Contracts notes | `contracts/README.md` |

Do not promote this extract into a unified authentication package without a separate human decision. Development Agents must not treat this directory as a standard write target.
