{
  "name": "@ooo/web-overview",
  "private": true,
  "description": "Canonical oorgos.org static overview. Edit here (Web/). Generate into this tree; sync to Community is a separate command.",
  "scripts": {
    "overview:links": "bash scripts/generate-overview.sh",
    "build": "npm run overview:links && node -e \"console.log('Static site ready in this Web directory. Publish from here, or npm run sync-to-community then deploy the Community mirror.')\"",
    "sync-to-community": "bash scripts/sync-to-community.sh",
    "sync-to-community:apply": "bash scripts/sync-to-community.sh --apply"
  }
}
