# extracts/

Review-only copies (`kind: extract`, `edit: false`). **Do not edit.**

| Path | Canonical home |
|------|----------------|
| `auth/` | `Core/` + `Community/` auth implementations |
| `console/` | `Core/apps/wire-console` (+ Core build scripts). Paths mirror Core: `console/apps/wire-console/`, `console/apps/shared/` |
| `provisional-canvas-web/` | reference branding only — not oorgos.org |
| `operations/` | gitignored local payload; canonical is `Core/deploy`, `Core/services`, `Core/scripts` |

These are frozen snapshots, so they fall behind. `./scripts/check-extract-drift.sh` reports how far, and each `EXTRACT-NOT-CANONICAL.md` records the last measured baseline.

See each subdirectory's `EXTRACT-NOT-CANONICAL.md` when present, and `PROJECT-MAP.yaml`.
