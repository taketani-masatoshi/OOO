# EXTRACT — not canonical

**Do not edit.** Thin branding extract from legacy canvas-web. Not the oorgos.org site.

Canonical Web product: `Web/`. Publish mirror: `Community/sites/coming-soon` (generated).

## Drift

No baseline and no source SHA, unlike the other extracts. Its source is a legacy
`canvas-web` tree that is not a path in any current git root, so there is nothing to
diff against and `scripts/check-extract-drift.sh` does not cover it. Treat the whole
tree as undated history — the branding file (`src/branding.ts`) is the only reason it
is kept.
